apiVersion: v1
kind: Pod
metadata:
  name: regservice.example.com
  labels:
    app: regservice
spec:
  containers:
  - name: regservice-1
    image: cybersmithio/regservice
    ports:
    - name: regserviceport
      containerPort: 443